"""
Module 06 — Files and Modules

For TODO 5, a helper.py file has been created next to this one — open it,
add the requested function, then import and use it here.
"""

# TODO 1: Write three lines of your choice to a file called "scratch.txt"
# using a with block (mode "w"). Each write() call should end with "\n".


# TODO 2: Read "scratch.txt" back with a with block and print the whole
# contents using .read().


# TODO 3: Read "scratch.txt" again, this time line by line with a for loop,
# printing each line with .strip() applied so there's no extra blank line.


# TODO 4: Create a comma-separated line like "math,90\nscience,85\nce,95"
# and write it to "grades.csv" (three separate lines). Then read it back,
# split each line on "," and print the two fields separately.


# TODO 5: Open helper.py, add a function `add(a, b)` that returns a + b.
# Then import it here (either `import helper` or `from helper import add`)
# and call it, printing the result of add(3, 4).


# TODO 6: Import the built-in `math` module and print math.sqrt(144) and
# math.pi rounded to 4 decimal places using an f-string.


# TODO 7: Import the built-in `random` module and print 5 random integers
# between 1 and 100 using a for loop and random.randint().
